// (C)2006 S2 Games
// inc.h
// 
// Support functions and macros
//=============================================================================

//=============================================================================
// TEXCOORDX
//=============================================================================
#if (CURRENT_COORD == 0)
	#undef CURRENT_COORD
	#define CURRENT_COORD	1
#elif (CURRENT_COORD == 1)
	#undef CURRENT_COORD
	#define CURRENT_COORD	2
#elif (CURRENT_COORD == 2)
	#undef CURRENT_COORD
	#define CURRENT_COORD	3
#elif (CURRENT_COORD == 3)
	#undef CURRENT_COORD
	#define CURRENT_COORD	4
#elif (CURRENT_COORD == 4)
	#undef CURRENT_COORD
	#define CURRENT_COORD	5
#elif (CURRENT_COORD == 5)
	#undef CURRENT_COORD
	#define CURRENT_COORD	6
#elif (CURRENT_COORD == 6)
	#undef CURRENT_COORD
	#define CURRENT_COORD	7
#elif (CURRENT_COORD == 7)
	#undef CURRENT_COORD
	#define CURRENT_COORD	8
#elif (CURRENT_COORD == 8)
	#undef CURRENT_COORD
	#define CURRENT_COORD	9
#elif (CURRENT_COORD == 9)
	#undef CURRENT_COORD
	#define CURRENT_COORD	10
#endif

//=============================================================================
// Global variables
//=============================================================================
